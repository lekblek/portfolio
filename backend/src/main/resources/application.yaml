spring:
  application:
    name: backend
  profiles:
    active:
      - dev


server:
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health

  endpoint:
    health:
      show-details: never
