spring:
  application:
    name: backend
  profiles:
    active:
      - dev

management:
  endpoints:
    web:
      exposure:
        include: health

  endpoint:
    health:
      show-details: never
