spring:
  application:
    name: backend
  profiles:
    active:
      - dev
  jackson:
#    default-property-inclusion: non_null


server:
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health

  endpoint:
    health:
      show-details: never
