package com.scalke.portfolio.backend.publication.domain.model;

/**
 * Deux types, un seul modèle (D02). Seul {@code ARTICLE} pourra appartenir à une série (étape 23).
 */
public enum PublicationType {
    ARTICLE,
    NEWS
}
