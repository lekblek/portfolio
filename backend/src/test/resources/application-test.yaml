spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.testcontainers: INFO
    com.github.dockerjava: WARN
